{
  "name": "mcp-walkthrough",
  "version": "0.3.1",
  "description": "MCP server for interactive code walkthroughs - Claude-driven code tours",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "mcp-walkthrough": "dist/index.js"
  },
  "files": [
    "dist",
    "scripts/postinstall.cjs",
    "vscode-extension/walkthrough-bridge.vsix",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Stefan-Nitu/mcp-walkthrough.git"
  },
  "author": "Stefan Nitu",
  "license": "MIT",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "walkthrough",
    "code-tour",
    "claude-code"
  ],
  "scripts": {
    "build": "npm run build:extension && tsc -p tsconfig.build.json",
    "build:extension": "node -e \"const fs=require('fs');const v=require('./package.json').version;const p=JSON.parse(fs.readFileSync('vscode-extension/package.json','utf-8'));p.version=v;fs.writeFileSync('vscode-extension/package.json',JSON.stringify(p,null,2)+'\\n')\" && cd vscode-extension && npm install && node esbuild.js && npx @vscode/vsce package --allow-missing-repository -o walkthrough-bridge.vsix",
    "postinstall": "node scripts/postinstall.cjs",
    "dev": "bun --watch src/index.ts",
    "start": "node dist/index.js",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "check": "bun run typecheck && bun run lint",
    "prepare": "husky"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.1",
    "msedge-tts": "^2.0.4",
    "pino": "^10.0.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "patchedDependencies": {
    "msedge-tts@2.0.4": "patches/msedge-tts@2.0.4.patch"
  }
}
